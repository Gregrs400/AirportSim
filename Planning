Multiple Airport program run process

- Planes get to gates

- Planes have flights generated per day

- Flights store the time they end, so next flight can be generated from that point

- All Passengers for day created before they enter airport, in 2d ArrayList
    - get transferred to paxInAirport when they arrive
    - passenger generation based on flights that will leave within the next 4 hours, stored in ArrayList
    - flights will be deleted from ArrayList as they fill up
    - need to figure out maximum time needed to get to gate, to determine minimum time of flight departure for passenger assignment

- Flights generated
- ArrayList of flights passed to paxArrival for generating movingPax 2d ArrayList
- Passengers generated, put in a Passenger master ArrayList, and in movingPax ArrayList

Different way to handle updating objects

- if object has not made it to destination, whatever that is, keep in list, being updated
- reduces copies and makes program run faster
- Problem: making a shallow copy
    - use Object object = new Object();
    - Object cloneObject = new Object(object);

Need to figure out how to generate flights
- Not sure how to determine the start time of flights
     - max amount of time it could take for a passenger to get to their gate
        - 85 minutes is maximum amount of time from drop off to at gate
        - need to consider how long it could take for the last passenger to be generated
        - give passengers of first flight 4 hours to make it to their plane, based on 4 hour flight departure passenger generation
        - flights on first day will depart at 4 AM at the earliest

Need to reserve seats correctly for flights to operate:
    - user should be able to pick seat if desired
    - passenger objects have seat chosen randomly
    - want passengers to only be able to randomly select seats unreserved, instead of searching through every seat for an unreserved one
    - want to use seat num, for instance 7A as reference to location in data structure: 7A would be row 6 col 0 of 2D ArrayList of seats
        - trying to figure out if I need HashMap at all
            - may not be worth space complexity, and size of each plane may not warrant the reduced time complexity
    - I want the order to be maintained to allow for easier display in a GUI at a later point
    - Also thinking about storing data multiple times and wanting to limit that
    - O(1) reservation, no need to search through list, when unreservedSeats is empty, all are reserved
    - passengers are not assigned to PlaneSeat objects unless they are sitting in a seat, so reserving is adding the passenger to the seating chart
        - unreservedSeats -> 1D String ArrayList with seat codes
            - when element is chosen, the seat code is used to find the seat in the seating chart and assign the passenger to that

Having a parent object name for everything may help with storing different objects throughout the airport in the Graph structure

